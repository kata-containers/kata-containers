[workspace]
members = [
    "kata-sys-util",
    "kata-types",
    "logging",
    "runtime-spec",
    "protocols",
    "safe-path",
    "shim-interface",
    "test-utils",
]
resolver = "2"

# Minimum buildable version:
# Keep in sync with version in versions.yaml
rust-version = "1.80.0"
