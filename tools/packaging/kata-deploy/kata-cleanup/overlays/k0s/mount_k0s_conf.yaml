apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kata-cleanup
  namespace: kube-system
spec:
  template:
    spec:
      volumes:
        - name: containerd-conf
          hostPath:
            path: /etc/k0s/containerd.d/
