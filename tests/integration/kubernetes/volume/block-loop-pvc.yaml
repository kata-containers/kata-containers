kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: block-loop-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-storage
  volumeMode: Block
  resources:
    requests:
      storage: CAPACITY
